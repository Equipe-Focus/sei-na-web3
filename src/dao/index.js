const modelDAO = require('./models');
const contractDAO = require('./contracts');
const documentDAO = require('./documents');
const blockchainDAO = require('./blockchains');

module.exports = {
    modelDAO,
    contractDAO,
    documentDAO,
    blockchainDAO,
};
